<h1>Create a New Category!</h1><br>

<%= form_for [:admin, @category] do |f| %>

  <%= f.label :search_term %>
  <%= f.text_field :search_term %>

  <%= f.submit %>

<% end %>
<br>
<%= link_to "Back to my admin page!", admin_user_path(current_user) %><br>
<br>

<%= link_to "Check out which categories already exist.", admin_categories_path %>
